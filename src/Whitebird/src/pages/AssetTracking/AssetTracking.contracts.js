/**
 * ASSET TRACKING - NEW API CONTRACTS
 * 
 * These endpoints do NOT exist in the current backend.
 */

/**
 * GET /api/Asset/{id}/timeline
 * 
 * Returns complete timeline/history for a single asset.
 * Supports: ?startDate=&endDate=&transactionType=
 */

/**
 * GET /api/Asset/tracking-list
 * 
 * Returns lightweight tracking list for asset selector.
 * Supports: ?search=&status=&categoryId=&holderId=&page=&pageSize=
 */